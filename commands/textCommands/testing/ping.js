module.exports = {
    name: "ping",
    description: "Ping",
    execute(message) {

        // const rackets = ["🪑", "💺", "🏓", "🗑️", "🍝", "🍞", "🥖", "🥪", "🥯", "🐕", "🎮"]
        // await message.reply ({
        //     content: "Pong! " + rackets[Math.floor(Math.random() * rackets.length)],
        //     ephemeral: true
        // })

        message.reply("no non è un ping");
    }
}